pipeline {
    agent any
    stages {
        stage(shellscriptoutput1) {
            steps {
                sh 'uptime'
            }    
        }
        stage(shellscriptoutput2) {
            steps {
                sh 'uname -a'
            }
        }
    }
}
